pg: &pg
  adapter: postgresql
  host: localhost
  port: 5432
  username: james
  password:
  schema_search_path: public
  encoding: utf8

development:
  <<: *pg
  database: checkout

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *pg
  database: checkout_test

production:
  <<: *pg
  database: checkout

# old database for migrating data
legacy:
  database: dbx2
  reconnect: false
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: webapp
  password: webapp
  socket: /tmp/mysql.sock
